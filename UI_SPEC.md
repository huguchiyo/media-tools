# 写真・動画ツール統合UI 仕様書（認識合わせ用）

Terminal ではなく UI 上で「写真と動画の分離」と「動画の YouTube アップロード」を実行するための仕様です。  
実装前に仕様レベルで認識を合わせることを目的としています。

---

## 前提・用語

- **camera**: 写真・動画が混在しているソースフォルダ（デフォルト: `G:/Users/chiyo/Pictures/camera`）
- **movie**: 動画を移動する先のルートフォルダ（`movie/2024`, `movie/2025` のように年ごとサブフォルダ）
- **分離**: camera 内の動画を movie へ移動・リネームし、写真は camera に残す（既存 `move_videos_to_movie.py` の動作）

---

## 1. 写真と動画の分離

### やりたいこと
指定したフォルダ（デフォルトは **camera**）の写真と動画を分離する。

### UI 仕様

| 項目 | 内容 |
|------|------|
| **操作** | 「分離を実行」のようなボタンで実行 |
| **ソースフォルダ** | テキスト入力 or フォルダ選択。**デフォルト**: `camera`（Pictures 直下の camera） |
| **移動先フォルダ** | テキスト入力 or フォルダ選択。**デフォルト**: `movie`（Pictures 直下の movie） |
| **実行前** | ドライラン（実際には移動しない）でプレビュー可能にすると親切（任意） |
| **実行時** | 既存スクリプトと同様「確認プロンプト」に相当する確認ダイアログ（実行してよいか）を出してから実行 |

### 実行する処理
- `Tools/VideoMoveTools/move_videos_to_movie.py` を `--source <ソース> --target <移動先> --execute`（または確認なしなら `--execute-yes`）で呼び出す。

---

## 2. 分離結果の表示

### やりたいこと
「どの動画がどのフォルダに移動したか」を表示する。

### UI 仕様

| 項目 | 内容 |
|------|------|
| **表示タイミング** | 分離実行後に自動表示。また「直近の分離結果を表示」のようなボタンでも表示可能 |
| **表示内容** | 直近 1 回の分離実行について、以下を表示する。 |
| **データソース** | `Tools/VideoMoveTools/moved_videos_log.json` の**直近 1 件**の実行レコード |

表示する項目（例）:

- **実行日時**
- **ソースフォルダ**・**移動先フォルダ**
- **移動数**
- **一覧**: 「移動元（相対パス or ファイル名）」→「移動先（`movie/年/ファイル名`）」の対応表

既存の `moved_videos_log.json` の `moved_files` に `source`, `dest`, `year`, `original_name`, `new_name` があるので、これを表形式で表示すればよい。

---

## 3. 動画のアップロード

### やりたいこと
**movie** に移動された動画を YouTube にアップロードするため、**どのフォルダをアップロードするか選択**して実行する。

### UI 仕様

| 項目 | 内容 |
|------|------|
| **対象** | `movie` 直下のサブフォルダ（年フォルダなど）。例: `movie/2024`, `movie/2025`, `movie/2026` |
| **操作** | フォルダ一覧を表示し、ユーザーが**1つ以上**選択 → 「アップロード実行」ボタンで実行 |
| **フォルダ一覧** | movie 直下のディレクトリを列挙。または、分離結果に登場した「年」だけに絞ってもよい（仕様で要確認） |
| **オプション** | 既存と同様、ドライラン（実際にはアップロードしない）を選べると親切（任意） |
| **実行時** | 選択フォルダごとに `youtube_upload.py --dir <フォルダ絶対パス>` を実行（必要なら `--no-fetch-list` も選択可能） |

### 実行する処理
- `Tools/youtube/youtube_upload.py` を `--dir "G:/Users/chiyo/Pictures/movie/2025"` のように呼び出す。
- 複数フォルダ選択時は、フォルダごとに順次実行する想定。

---

## 4. アップロード結果の表示

### やりたいこと
「どの動画がアップロードされたか」を表示する。

### UI 仕様

| 項目 | 内容 |
|------|------|
| **表示タイミング** | アップロード実行後に自動表示。また「直近のアップロード結果を表示」のようなボタンでも表示可能 |
| **表示内容** | 直近のアップロード実行について、以下を表示したい。 |
| **データソース** | 現状は `Tools/youtube/data/upload_runs.json` の**直近 1 件**（または複数件） |

**現状の `upload_runs.json` に含まれる情報（サマリのみ）:**

- 実行日時、対象ディレクトリ、年、ドライラン可否
- **成功数**・**失敗数**・**スキップ数**・**プレイリスト追加数**

**要望:** 「どの動画がアップロードされたか」＝ **動画単位の一覧** を表示したい。

- **課題**: 現在の `upload_runs.json` には「ファイル名／タイトルごとの成功・失敗」は入っていない。
- **対応案（実装時にいずれか選択）**  
  1. **youtube 側を拡張**: 実行結果に「アップロードした動画のタイトル（と動画ID）」の一覧を返す、または `data/` に実行ごとの詳細 JSON（例: `upload_run_details_YYYYMMDD_HHMMSS.json`）を書き出す。  
  2. **UI 側のみ**: まずはサマリ（成功数・失敗数・スキップ数）と対象フォルダ・日時だけ表示し、「詳細はログファイルを参照」と案内。必要なら後から 1 を追加。

**表示項目（最低限）:**

- 実行日時、対象フォルダ（`movie/YYYY`）
- 成功数・失敗数・スキップ数・プレイリスト追加数

**表示項目（要望・実装可能なら）:**

- アップロードされた動画のタイトル（またはファイル名）の一覧

---

## UI 構成案（ワン画面）

- **セクション 1: 写真と動画の分離**
  - ソースフォルダ（デフォルト: camera）、移動先（デフォルト: movie）の入力
  - 「分離を実行」ボタン（確認ダイアログあり）
  - （任意）「プレビュー（ドライラン）」ボタン

- **セクション 2: 分離結果**
  - 「直近の分離結果を表示」ボタン ＋ 結果エリア（表形式: 移動元 → 移動先、実行日時・移動数）

- **セクション 3: 動画のアップロード**
  - movie 直下のフォルダ一覧（チェックボックス or リストで複数選択）
  - 「アップロード実行」ボタン（確認ダイアログあり）
  - （任意）「ドライラン」オプション

- **セクション 4: アップロード結果**
  - 「直近のアップロード結果を表示」ボタン ＋ 結果エリア（サマリ＋できれば動画一覧）

---

## 技術的な注意点（実装時）

- **パス**: camera / movie のデフォルトは `G:/Users/chiyo/Pictures/camera` と `G:/Users/chiyo/Pictures/movie`。**設定画面から変更可能**にする。
- **実行環境**: 既存スクリプトは Python。UI からは `subprocess` 等でこれらの Python スクリプトを呼び出す想定。
- **エラー表示**: スクリプトの標準エラー出力や終了コードをキャッチし、UI 上でエラーメッセージを表示する。
- **非同期**: 分離・アップロードは時間がかかるため、実行中は「実行中」表示＋キャンセル（任意）があるとよい。
- **実行時ログのリアルタイム表示**: 実行中はダイアログ（またはパネル）でスクリプトの標準出力・標準エラーを**リアルタイム**で流し表示する。実装例: subprocess でプロセス起動 → stdout/stderr をパイプで読み、1行ずつ（またはチャンクずつ）フロントへ送信（WebSocket / SSE / ポーリング）して追記表示。モックでは同様の見た目を再現済み。

---

## 確認済み事項（2026-03 反映）

1. **デフォルトの camera / movie**  
   → よい。かつ**設定からパスを変更できる**ようにする。

2. **分離の「確認」**  
   → 確認ダイアログを出す。オプションで「確認なしで実行」も選べるようにする。

3. **アップロード対象フォルダ**  
   → **movie 直下の全サブフォルダを一覧**して、その中から選択する。

4. **アップロード結果の「どの動画が」**  
   → **最初はサマリのみ**でよい（成功数・失敗数・スキップ数など）。

5. **UI の形態**  
   → デスクトップ or ローカル Web で検討。**まずはモックで表示イメージを確認**してから決める。

---

## デスクトップ vs ローカル Web（使い勝手の目安）

| 観点 | デスクトップ（Tkinter/PyQt 等） | ローカル Web（Flask + ブラウザ） |
|------|----------------------------------|----------------------------------|
| **見た目・レイアウト** | やや制約あり、OS 依存 | CSS で自由にデザインしやすい |
| **起動** | アプリを直接起動 | ブラウザで `http://localhost:xxxx` を開く |
| **フォルダ選択** | ネイティブのフォルダ選択ダイアログが使いやすい | パスを手入力 or ファイル API で選択 |
| **配布・環境** | Python + GUI ライブラリが必要 | Python + ブラウザがあればよい |
| **実装の手軽さ** | 1 つの Python で完結 | HTML/CSS/JS + サーバー側 Python の 2 層 |

**おすすめ**: まず **ローカル Web のモック（静的 HTML）** でレイアウトを確認し、気に入ればそのまま Flask 等で「実際に実行する版」に発展させるのがやりやすいです。フォルダ選択は「パスをテキストで入力＋設定に保存」で揃えれば、どちらでも同じように扱えます。

---

## モック

- **目的**: 実際にスクリプトは実行せず、「こんな表示・操作になる」を確認するため。
- **中身**: 静的 HTML（＋必要なら少し JS）で、各セクションの見た目とボタン配置を再現。分離結果・アップロード結果は**ダミーデータ**で表示。
- **ファイル**: `Tools/mock_ui.html` を参照。
